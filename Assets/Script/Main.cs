using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;
using UnityEngine.Profiling;
using System;
using Newtonsoft.Json;

public class Main : MonoBehaviour
{
    // Use this for initialization
    void Start()
    {
        //ConfigBinaryManager.Instance.Init();
        ////var obj = JsonConvert.DeserializeObject<ConfigJsonContainer>(text, settings);
        ////var obj2 = obj.dataList[0] as CfgTest;
        ////Debug.LogError(obj.dataList.Count);
        //var time = Time.realtimeSinceStartup;
        //for (int i = 0; i < 1000; i++)
        //{
        //    ConfigSOManager.Instance.Init();
        //}
        //Debug.LogError(Time.realtimeSinceStartup - time);
        //time = Time.realtimeSinceStartup;
        //for (int i = 0; i < 1000; i++)
        //{
        //    ConfigTextManager.Instance.Init();
        //}
        //Debug.LogError(Time.realtimeSinceStartup - time);
        //time = Time.realtimeSinceStartup;
        //for (int i = 0; i < 1000; i++)
        //{
        //    ConfigBinaryManager.Instance.Init();
        //}
        //Debug.LogError(Time.realtimeSinceStartup - time);
        //time = Time.realtimeSinceStartup;
        //for (int i = 0; i < 1000; i++)
        //{
        //    ConfigJsonManager.Instance.Init();
        //}
        //Debug.LogError(Time.realtimeSinceStartup - time);
    }

    // Update is called once per frame
    void Update () {
		
	}
}
